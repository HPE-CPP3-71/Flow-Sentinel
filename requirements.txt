## Flow-Sentinel Python dependencies
# Note: `tkinter` is part of the Python standard library (OS package),
# and `customtkinter` is the pip package used for the UI.

customtkinter
nfstream
joblib
numpy
pandas
scikit-learn
xgboost
# OSPF rule-based detection parses packets with Scapy. Imported defensively in
# backend/rule_based.py — the app still runs (OSPF detection disabled) if absent.
scapy

# Optional / troubleshooting helpers (unpin to let pip resolve compatible versions)
# matplotlib
# python-dateutil